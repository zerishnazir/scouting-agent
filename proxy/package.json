{
  "name": "startup-scout-proxy",
  "version": "1.0.0",
  "description": "Lightweight proxy server for Startup Scout — forwards LLM requests server-side so the API key is never exposed to the browser.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
